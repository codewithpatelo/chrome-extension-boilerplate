const should = require('chai').should();

const myPackage = require('../src/index.js');


describe('TEST1', () => {
  it('If ... is ... it should ...', () => {
	//result = myPackage.myFunction();
    //result.should.equal('');
  });
});

describe('TEST2', () => {
  it('If ... is ... it should ...', () => {
	//result = myPackage.myFunction();
    //result.should.equal('');
  });
});

describe('TEST3', () => {
  it('If ... is ... it should ...', () => {
	//result = myPackage.myFunction();
    //result.should.equal('');
  });
});
